# নেটওয়ার্ক প্যাকেট স্নিফার ওয়েব ড্যাশবোর্ড
## মেশিন লার্নিং ভিত্তিক নেটওয়ার্ক ট্রাফিক বিশ্লেষণ সিস্টেম

**প্রজেক্টের নাম:** Network Packet Sniffer Web Dashboard with ML-based Intrusion Detection  
**উপস্থাপক:** [আপনার নাম]  
**তারিখ:** ২০২৬

---

## ১. ভূমিকা (Introduction)

### নেটওয়ার্ক প্যাকেট স্নিফার কী?

নেটওয়ার্কে যখন কোনো ডেটা এক কম্পিউটার থেকে আরেক কম্পিউটারে যায়, তখন সেটি ছোট ছোট **প্যাকেটে** ভাগ হয়ে যায়। এই প্যাকেটগুলো ধরা এবং পড়ার প্রক্রিয়াকে **প্যাকেট স্নিফিং** বলা হয়।

**Packet Sniffer** হলো একটি সফটওয়্যার টুল যা নেটওয়ার্কের ট্রাফিক রিয়েল-টাইমে পর্যবেক্ষণ করে।

### কেন এটি গুরুত্বপূর্ণ?

- **সাইবার সিকিউরিটি:** অস্বাভাবিক ট্রাফিক শনাক্ত করে হ্যাকার বা আক্রমণ থেকে রক্ষা করে
- **নেটওয়ার্ক ট্রাবলশুটিং:** নেটওয়ার্কের সমস্যা খুঁজে বের করতে সাহায্য করে
- **ডেটা মনিটরিং:** কোন ডিভাইস কতটুকু ব্যান্ডউইথ ব্যবহার করছে তা জানা যায়

---

## ২. প্রজেক্টের উদ্দেশ্য (Project Objective)

এই প্রজেক্টের মূল লক্ষ্য হলো এমন একটি সিস্টেম তৈরি করা যা:

- ✅ রিয়েল-টাইমে নেটওয়ার্ক প্যাকেট ক্যাপচার করবে
- ✅ ওয়েব ব্রাউজারে লাইভ ড্যাশবোর্ড দেখাবে
- ✅ মেশিন লার্নিং দিয়ে প্রতিটি প্যাকেট **"Normal"** বা **"Suspicious"** হিসেবে চিহ্নিত করবে
- ✅ প্রোটোকল ও IP ঠিকানা দিয়ে ফিল্টার করার সুবিধা দেবে
- ✅ ডেটা CSV ফাইলে এক্সপোর্ট করার সুবিধা দেবে

---

## ৩. কীভাবে কাজ করে (How It Works)

### ধাপ-১: প্যাকেট ক্যাপচার (`sniffer.py`)

```
ইন্টারনেট ট্রাফিক → Scapy/Raw Socket → প্যাকেট ধরা
```

- `sniffer.py` ফাইলটি নেটওয়ার্ক কার্ড থেকে প্যাকেট পড়ে
- প্রতিটি প্যাকেট থেকে বের করা হয়: Source IP, Destination IP, Protocol (TCP/UDP/ICMP), Packet Size, Timestamp
- যদি Admin Permission না থাকে, **Simulation Mode** চালু হয় (ডেমোর জন্য ফেক প্যাকেট তৈরি করে)

### ধাপ-২: ডেটা প্রসেসিং (`analyzer.py`)

```
প্যাকেট → PacketAnalyzer → ML Prediction → Statistics আপডেট
```

- প্যাকেট আসার সাথে সাথে Analyzer এটি সংরক্ষণ করে
- ML মডেল দিয়ে **Prediction** করে (Normal/Suspicious)
- মোট প্যাকেট সংখ্যা, প্রোটোকল বিতরণ ইত্যাদি হিসাব রাখে

### ধাপ-৩: ML Prediction (`ml_model.py`)

```
প্যাকেট Features → RandomForest Model → Normal / Suspicious
```

- Protocol এবং Packet Size দিয়ে ML মডেল সিদ্ধান্ত নেয়
- বড় প্যাকেট বা ICMP Flood → **Suspicious**
- স্বাভাবিক TCP/UDP → **Normal**

### ধাপ-৪: WebSocket দিয়ে Frontend আপডেট (`app.py`)

```
Flask Server → Socket.IO → Browser Dashboard
```

- প্রতিটি নতুন প্যাকেট ওয়েবসকেটের মাধ্যমে ব্রাউজারে পাঠানো হয়
- পেজ রিফ্রেশ ছাড়াই টেবিল আপডেট হয়

### ধাপ-৫: ড্যাশবোর্ড ভিজ্যুয়ালাইজেশন (`index.html + script.js`)

```
Browser → Chart.js → Protocol Chart + Threat Ratio Chart
```

- প্রোটোকল ডিস্ট্রিবিউশন দেখায় (Doughnut Chart)
- Normal vs Suspicious অনুপাত দেখায় (Pie Chart)
- প্যাকেট টেবিলে রঙিন badge দেখায়

---

## ৪. সিস্টেম আর্কিটেকচার (System Architecture)

```
┌─────────────────────────────────────────────┐
│              নেটওয়ার্ক ট্রাফিক               │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
        ┌──────────────────┐
        │   sniffer.py     │  ← প্যাকেট ক্যাপচার (Scapy / Raw Socket)
        └────────┬─────────┘
                 │
                 ▼
        ┌──────────────────┐
        │   models.py      │  ← Packet Data Model (src_ip, dst_ip, protocol, size)
        └────────┬─────────┘
                 │
                 ▼
        ┌──────────────────┐
        │   analyzer.py    │  ← Statistics + ML Prediction কল করে
        └────────┬─────────┘
                 │
       ┌─────────┴──────────┐
       │                    │
       ▼                    ▼
┌────────────┐      ┌────────────────┐
│ ml_model.py│      │  filters.py    │
│ (Random    │      │  exporter.py   │
│  Forest)   │      │  (CSV export)  │
└─────┬──────┘      └───────┬────────┘
      │                     │
      └──────────┬──────────┘
                 │
                 ▼
        ┌──────────────────┐
        │     app.py       │  ← Flask + Socket.IO Server
        └────────┬─────────┘
                 │ WebSocket (Polling)
                 ▼
        ┌──────────────────┐
        │  Web Dashboard   │  ← HTML + CSS + Chart.js + JavaScript
        │  (Browser)       │
        └──────────────────┘
```

### মডিউল পরিচিতি

| ফাইল | কাজ |
|------|-----|
| `sniffer.py` | নেটওয়ার্ক থেকে প্যাকেট ধরে |
| `models.py` | প্যাকেটের ডেটা স্ট্রাকচার |
| `analyzer.py` | পরিসংখ্যান ও ML Prediction |
| `ml_model.py` | মডেল ট্রেনিং ও Prediction |
| `filters.py` | Protocol/IP/Prediction ফিল্টার |
| `exporter.py` | CSV ফাইলে ডেটা সেভ |
| `app.py` | Flask Web Server ও API |
| `templates/index.html` | ড্যাশবোর্ড UI |
| `static/script.js` | রিয়েল-টাইম আপডেট লজিক |
| `static/style.css` | ড্যাশবোর্ড ডিজাইন |

---

## ৫. মেশিন লার্নিং অংশ (ML Part)

### ডেটাসেট

- **ফাইল:** `data/training_data.csv`
- **মোট রো:** ২৫০টি
- **কলাম:** `src_ip, dst_ip, protocol, packet_size, label`
- **ব্যালেন্স:** ১২৫টি Normal + ১২৫টি Suspicious (৫০/৫০)

### ডেটার প্যাটার্ন

| ধরন | Protocol | Packet Size |
|-----|----------|-------------|
| **Normal** | TCP, UDP (বেশি), ICMP (কম) | ১০০ – ১০০০ bytes |
| **Suspicious** | ICMP (বেশি), বড় TCP/UDP | ১৪০০ – ৩০০০ bytes |

### মডেল

- **Algorithm:** `RandomForestClassifier` (Scikit-learn)
- **Features:** Protocol (encoded), Packet Size
- **Accuracy:** ১০০% (Test Set এ)
- **সংরক্ষণ:** `data/model.joblib` (joblib দিয়ে)

### কেন ML ব্যবহার করা হয়েছে?

- Rule-based সিস্টেম সব ধরনের আক্রমণ ধরতে পারে না
- ML মডেল প্যাটার্ন শিখে নতুন অস্বাভাবিক ট্রাফিকও চিনতে পারে
- রিয়েল-টাইমে মিলিসেকেন্ডে Prediction দিতে পারে

---

## ৬. প্রধান ফিচারসমূহ (Features)

- 🟢 **রিয়েল-টাইম প্যাকেট ক্যাপচার** — Start বাটন ক্লিক করলেই শুরু
- 📊 **লাইভ ড্যাশবোর্ড** — পেজ রিফ্রেশ ছাড়াই আপডেট হয়
- 🤖 **ML Prediction** — প্রতিটি প্যাকেট Normal/Suspicious চিহ্নিত হয়
- 🔍 **ফিল্টারিং** — Protocol, IP ও Prediction দিয়ে খোঁজা যায়
- ⬇️ **CSV Export** — pandas দিয়ে ডেটা ফাইলে সেভ করা যায়
- 🎨 **রঙিন Badge** — TCP=নীল, UDP=সবুজ, ICMP=কমলা, Suspicious=লাল
- 🛡️ **Tooltip** — কেন Suspicious সেটি ব্যাখ্যা দেয় (hover করলে)
- 🟡 **Simulation Mode** — Admin Permission না থাকলেও কাজ করে

---

## ৭. ব্যবহৃত প্রযুক্তি (Tech Stack)

| প্রযুক্তি | উদ্দেশ্য |
|-----------|----------|
| **Python 3** | মূল প্রোগ্রামিং ভাষা |
| **Flask** | Web Server ও API |
| **Flask-SocketIO** | রিয়েল-টাইম WebSocket |
| **Scapy** | প্যাকেট ক্যাপচার |
| **Scikit-learn** | ML মডেল (Random Forest) |
| **Pandas** | ডেটা প্রসেসিং ও CSV Export |
| **Joblib** | ML মডেল সেভ/লোড |
| **Chart.js** | ইন্টারেক্টিভ চার্ট |
| **HTML/CSS/JS** | ওয়েব ড্যাশবোর্ড UI |

---

## ৮. বাস্তব জীবনে প্রয়োগ (Real-World Applications)

- 🏢 **কর্পোরেট নেটওয়ার্ক সুরক্ষা** — অফিসের নেটওয়ার্ক পর্যবেক্ষণ
- 🏫 **শিক্ষা প্রতিষ্ঠান** — Networking শেখানোর হাতিয়ার
- 🔒 **Intrusion Detection System (IDS)** — হ্যাকার শনাক্ত করা
- 🌐 **ISP মনিটরিং** — ইন্টারনেট সেবাদাতা প্রতিষ্ঠানের ট্রাফিক বিশ্লেষণ
- 🔬 **Cybersecurity Research** — গবেষণা ও পরীক্ষা-নিরীক্ষা

---

## ৯. সীমাবদ্ধতা (Limitations)

- ⚠️ ML মডেল Synthetic (কৃত্রিম) ডেটায় ট্রেন করা — বাস্তব আক্রমণ শনাক্তে সীমা থাকতে পারে
- ⚠️ রিয়েল প্যাকেট ক্যাপচারে **Administrator Permission** প্রয়োজন
- ⚠️ এটি Production-level IDS নয়, বরং একটি শিক্ষামূলক প্রজেক্ট
- ⚠️ WebSocket এর বদলে HTTP Polling ব্যবহার করা হয়েছে (Werkzeug সীমাবদ্ধতা)
- ⚠️ শুধু IPv4 প্যাকেট সমর্থিত

---

## ১০. ভবিষ্যৎ উন্নতির সুযোগ (Future Improvements)

- 🚀 **Deep Learning মডেল** (LSTM বা CNN) ব্যবহার করে আরও নির্ভুল Prediction
- ☁️ **Cloud Deployment** — AWS বা Azure তে হোস্ট করা
- 📧 **Alert System** — Suspicious ট্রাফিক পেলে Email বা SMS পাঠানো
- 🌍 **IPv6 Support** — আধুনিক নেটওয়ার্কের জন্য
- 📦 **Real Dataset** — CIC-IDS2017 বা NSL-KDD দিয়ে মডেল পুনরায় ট্রেন করা
- 🔐 **User Authentication** — লগইন সিস্টেম যুক্ত করা

---

## ১১. উপসংহার (Conclusion)

এই প্রজেক্টে আমরা একটি সম্পূর্ণ **ওয়েব-ভিত্তিক নেটওয়ার্ক প্যাকেট স্নিফার** তৈরি করেছি যেখানে **মেশিন লার্নিং** ব্যবহার করে রিয়েল-টাইমে ট্রাফিক বিশ্লেষণ করা হয়।

প্রজেক্টটি নেটওয়ার্কিং, সাইবার সিকিউরিটি এবং মেশিন লার্নিং — এই তিনটি গুরুত্বপূর্ণ বিষয়কে একত্রিত করে একটি কার্যকর সিস্টেম তৈরি করেছে। এটি শিক্ষার্থীদের নেটওয়ার্ক সিকিউরিটি সম্পর্কে বাস্তব ধারণা দিতে সক্ষম।

---

---

# 🎤 উপস্থাপনা বক্তব্য (Presentation Speech Script)

> *নিচের অংশটি ভাইভা বা ক্লাস প্রেজেন্টেশনে বলার জন্য লেখা হয়েছে। স্বাভাবিকভাবে পড়ুন।*

---

### শুরুর কথা

"আসসালামু আলাইকুম স্যার/ম্যাডাম। আমি আজকে আমাদের প্রজেক্ট **"Network Packet Sniffer Web Dashboard with Machine Learning"** সম্পর্কে উপস্থাপন করব।"

---

### প্রজেক্ট কী সেটা বলা

"স্যার, আমরা জানি যে ইন্টারনেটে যখন কোনো ডেটা পাঠানো হয়, সেটা ছোট ছোট 'প্যাকেটে' ভাগ হয়ে যায়। এই প্যাকেটগুলো ধরে বিশ্লেষণ করার প্রক্রিয়াকে বলে **Packet Sniffing**।

আমাদের প্রজেক্টে আমরা এমন একটি সিস্টেম তৈরি করেছি যেটা রিয়েল-টাইমে এই প্যাকেটগুলো ধরে, বিশ্লেষণ করে, এবং একটি সুন্দর ওয়েব ড্যাশবোর্ডে দেখায়।

সবচেয়ে গুরুত্বপূর্ণ বিষয় হলো — আমরা এখানে **Machine Learning** যোগ করেছি যেটা প্রতিটি প্যাকেটকে **Normal** বা **Suspicious** হিসেবে চিহ্নিত করে।"

---

### কীভাবে কাজ করে সেটা বলা

"স্যার, সিস্টেমটি কয়েকটি ধাপে কাজ করে —

**প্রথম ধাপে**, `sniffer.py` ফাইলটি Python এর Raw Socket ব্যবহার করে নেটওয়ার্ক থেকে প্যাকেট ধরে। প্রতিটি প্যাকেট থেকে বের করা হয় — কোথা থেকে আসছে (Source IP), কোথায় যাচ্ছে (Destination IP), কোন প্রোটোকল (TCP/UDP/ICMP), এবং প্যাকেটের আকার।

**দ্বিতীয় ধাপে**, `analyzer.py` এই প্যাকেটটিকে আমাদের **Machine Learning মডেলে** পাঠায়। মডেলটি একটি **Random Forest Classifier** যেটাকে আমরা ২৫০টি লেবেলকৃত প্যাকেট ডেটা দিয়ে ট্রেন করেছি।

মডেলটি দেখে — প্যাকেটের আকার কত বড়? প্রোটোকল কী? যদি প্যাকেট অনেক বড় হয় বা অনেক বেশি ICMP প্যাকেট আসে — তাহলে সেটাকে **Suspicious** বলে চিহ্নিত করে।

**তৃতীয় ধাপে**, Flask ও Socket.IO ব্যবহার করে এই ডেটা রিয়েল-টাইমে ব্রাউজারে পাঠানো হয়। ব্রাউজারে লাইভ টেবিলে প্যাকেটগুলো দেখা যায় এবং Chart.js দিয়ে সুন্দর চার্ট তৈরি হয়।"

---

### ফিচার সম্পর্কে বলা

"স্যার, আমাদের ড্যাশবোর্ডে আছে —

- **Start ও Stop বাটন** দিয়ে ক্যাপচার নিয়ন্ত্রণ করা যায়
- প্যাকেট টেবিলে **রঙিন badge** — TCP নীল, UDP সবুজ, ICMP কমলা রঙে দেখায়
- Suspicious প্যাকেটের সারি **লাল রঙে** হাইলাইট হয়
- badge-এ mouse রাখলে **Tooltip** দেখায় — কেন এটি Suspicious সেটা ব্যাখ্যা করে
- Protocol ও IP দিয়ে **ফিল্টার** করা যায়
- সব ডেটা **CSV ফাইলে Export** করা যায়
- দুটি চার্ট আছে — একটি Protocol Distribution, আরেকটি Normal vs Suspicious অনুপাত দেখায়"

---

### ML সম্পর্কে প্রশ্ন আসতে পারে — এভাবে বলুন

"স্যার যদি জিজ্ঞেস করেন ML কেন ব্যবহার করলাম —

Traditional Rule-based সিস্টেমে আমাদের নিজেদের বলে দিতে হয় — 'এত বড় প্যাকেট আসলে Suspicious'। কিন্তু ML মডেল নিজে থেকে ডেটা দেখে **প্যাটার্ন শেখে**। ফলে এটি এমন আক্রমণও ধরতে পারে যেটা আমরা আগে থেকে বলিনি।

আমাদের **Random Forest** মডেলটি টেস্ট ডেটায় **১০০% নির্ভুলতা** অর্জন করেছে।"

---

### সীমাবদ্ধতা স্বীকার করা

"স্যার, অবশ্যই এই প্রজেক্টের কিছু সীমাবদ্ধতা আছে।

আমাদের ML মডেল Synthetic ডেটায় ট্রেন করা, তাই বাস্তব আক্রমণে এটি সবসময় নির্ভুল নাও হতে পারে। এছাড়া রিয়েল প্যাকেট ক্যাপচার করতে Administrator Permission দরকার। সেজন্য আমরা একটি **Simulation Mode** রেখেছি যেটা Admin Permission ছাড়াও ডেমো দিতে পারে।

ভবিষ্যতে আমরা এখানে Deep Learning মডেল ও বাস্তব ডেটাসেট যোগ করতে পারব।"

---

### শেষ কথা

"স্যার, সংক্ষেপে বলতে গেলে — এই প্রজেক্টটি নেটওয়ার্কিং, ওয়েব ডেভেলপমেন্ট এবং মেশিন লার্নিং তিনটি বিষয়কে একসাথে কাজে লাগিয়ে একটি বাস্তব সাইবার সিকিউরিটি টুল তৈরি করেছে।

এটি তৈরি করতে গিয়ে আমরা Python, Flask, Scapy, Scikit-learn এবং JavaScript সম্পর্কে অনেক কিছু শিখতে পেরেছি।

আপনার কোনো প্রশ্ন থাকলে আমি উত্তর দিতে প্রস্তুত। ধন্যবাদ।"

---

*— সমাপ্ত —*
